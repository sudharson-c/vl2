# Method 2: SSH (scp)
# On Receiver VM:
sudo apt-get update
sudo apt-get install -y openssh-server
sudo systemctl enable --now ssh
sudo systemctl status ssh --no-pager

# On Sender VM (replace user, ip):
scp ~/file.txt user@192.168.100.5:/home/user/
